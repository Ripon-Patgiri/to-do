import Task from "./task";

export default class UI {
  static loadHomePage() {
    //static method : a method defined directly on a class and can be called without creating an instance of the class

    
  }
}
